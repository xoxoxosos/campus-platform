# ==================== 开发环境配置 ====================

spring:
  # MySQL 连接地址
  datasource:
    url: jdbc:mysql://localhost:3307/campus-platform?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 2005

  # Redis 连接地址
  data:
    redis:
      host: localhost
      port: 6379
      password:           # 开发环境通常无密码

# 日志级别
logging:
  level:
    com.xiuhui.campus: debug
    org.springframework.security: debug
